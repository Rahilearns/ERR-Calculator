# Working / reference files (not part of the deployed site)
*.xlsx
*.pdf
*.docx
*.png
~$*
Prompts.*

# ...but DO ship the downloadable sample assets
!assets/
!assets/*.xlsx

# Backend (auth server) — never commit deps, secrets, or the user store (password hashes)
node_modules/
.env
server/.env
server/data/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code config
.claude/
~WRL*.tmp
